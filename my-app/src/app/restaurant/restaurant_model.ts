export interface restaurant_model {
  name: string;
  phone: number;
  address: string;
  cuisine: string;
  is_breakast: boolean;
  is_fastfood: boolean;
  preference: number;
  city: string;
  not_seen: boolean;
}

